#include <stdio.h>
int main(){
    float a, b;
    printf("sisair la valeur de a:");
    scanf("%f",&a);
    printf("sisair la valeur de b:");
    scanf("%f",b);
    printf("a+b=%2.f\n",a+b );
    printf("a-b=%2.f\n",a-b);
    printf("a*b=%2.f\n",a*b);
    return 0;
}